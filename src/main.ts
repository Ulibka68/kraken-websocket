// import { delayMillis, greet } from './helpers/helper';
export { greet, foo } from './helpers/helper';
/*

export const foo = async (num?: number): Promise<boolean> => {
  greet('World ' + num);
  await delayMillis(1000);
  console.log('done');
  return true;
};
*/
